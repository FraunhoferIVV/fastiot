
fastiot_service:
  name: dash

  mount_config_dir: required

  ports: # List all ports of the module
    fastapi:
      port_nr: 5801
      env_variable: "EXAMPLE_SAM_DASH_PORT"

  depends_on:
    - nats

  platforms:
    - amd64

  additional_pip_packages:
    - dash~=2.6.1
    - plotly~=5.9.0
